





1. 基础设施部(基础与工具)   inner_type.h ln_common.h/.cpp cJSON.h/.cpp                                 提供全局数据类型、日志打印工具和配置文件解析能力。,任何项目都需要地基。为了让所有文件说话（数据类型）一致，并能打印漂亮的调试日志。
2. 外交通讯部(网络传输)     ln_socket.h/.cpp ln_udp.h/.cpp ln_probe_process.h/.cpp                     负责网络的监听。利用 UDP 广播让外界搜到板子，利用 TCP 接收控制命令。,板子不能是孤岛，必须和网络上的其他主机（PC）通信，所以需要封装网络接口。
3. 仓储物流部(缓存与队列)   ln_buffer.h/.cpp,维护一个内存队列，负责媒体数据块的申请、存放和释放。           **关键！**摄像头产生数据（写）和网络发数据（读）的速度不匹配，必须有一个“蓄水池”来缓冲。
4. 业务执行部(核心控制)     ln_main.cpp cmd_process.h/.cpp media_process.h/.cpp ln_test_file.h/.cpp    系统的核心大脑。解析具体命令（如“开始录像”），并把视频文件或摄像头数据推流出去。,收到命令后总得有人去干活，比如去读文件（模拟摄像头）并将数据打包发送。




彻底搞懂了 UDP（网络探测）、TCP（命令信道）和 Buffer（数据环形队列），你就已经啃下了这个项目 70% 以上最硬核、最具有通用价值的骨架！ 掌握了这三块，你不仅能轻松写出这个项目的 main 和 Makefile



底层基石（数据类型与工具）：inner_type.h 和 ln_common.h
为什么需要：网络传输必须保证数据长度绝对精确。比如在 inner_type.h 里面把 unsigned int 定义为 HI_U32。你不需要去背它，只需要把它当成一个“数字字典”，用的时候去查就行。

中层骨架（核心三大件）：ln_socket + ln_udp + ln_buffer
为什么需要：这是你已经锁定的核心。它们不关心传输的具体是文本、图片还是视频，它们只负责建立连接、传输字节、安全排队   。                      

上层业务（你看到的读取视频文件的 .cpp）：ln_test_file.cpp 和 media_process.cpp
为什么需要：你看到的“读取视频文件并定义视频大小”的文件（即 ln_test_file.cpp），在当前这个案例中扮演的是“模拟摄像头”的角色。因为这个案例为了让所有人都能跑通，它没有强制你接一个真实的物理摄像头，而是用代码去读取工程目录下的 bigbuckbunny_720x480.h265 这个本地视频文件。它读取出每一帧，并打上分辨率（720x480）的标签，然后塞进 Buffer 队列里。

未来的改写：当你将来在 RK3568 上做 YOLO 项目时，你会把这个 ln_test_file.cpp 直接扔掉，换成你自己写的 rk_camera_v4l2.cpp（利用 V4L2 接口真正控制板子上的摄像头）。但无论你怎么换，最后拿到的图像数据，依然要老老实实地塞进你写好的 Buffer 队列里。













